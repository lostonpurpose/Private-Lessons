<div class="container w-100">
  <div class="row">
    <div class="column">
      <%= image_tag("anri.jpg", width: "200px") %>
    <h2><%= @user.name %></h2>
    <p><%= @user.description %><p>
    </div>
  </div>
  <% if @user == current_user %>
  <%= link_to "Update profile", user_path, "#", class: "btn btn-gradient" %>
  <% end %>
</div>
