<div>
  <%= link_to "Create class", new_event_path(@events), class: "btn btn-custom" %>
</div>
<%= render "/shared/event_card" %>
<div >
  <%# <%= link_to "Add Video", , class: "btn btn-custom" %>
  <!-- Button trigger modal -->
  <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#exampleModal">
    Add Video to Event
  </button>
  <!-- Modal -->
  <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h1 class="modal-title fs-5" id="exampleModalLabel">Add Video to Event</h1>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          Select a video file then click update button.
          <%= simple_form_for @event do |f| %>
            <% f.input :video %>
            <%= f.submit "Select", class: "btn btn-custom my-3" %>
          <% end %>
          <%= f.input :title %>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
          <button link_to edit_event_path type="button" class="btn btn-primary">Upload video</button>
        </div>
      </div>
    </div>
  </div>
</div>
<div class="container w-100">
  <div class="row">
    <div class="column">
      <p class="mb-2 mt-4 d-flex align-items-center fw-bold">Who is joining?</p>
      <div>       <%# Iterating through attending users % %>
        <% @bookings.each do |booking| %>
          <div class="d-flex justify-content-between align-items-center">
            <div class="d-flex align-items-center" >
              <%= link_to user_path(booking.user) do %>
                <%= cl_image_tag booking.user.photo.key, class: "avatar-bordered me-2", alt: "avatar-bordered" %>
              <% end %>
              <span><%= booking.user.name %></span>
            </div>
            <span><%= booking.state %></span>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
