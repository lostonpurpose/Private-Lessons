<div class="container w-100 mb-5">
  <div class="row">
    <div class="column">
      <h2 class="my-3 fw-bold d-flex justify-content-center">Welcome <%= current_user.name.capitalize %>!</h2>
      <p class="my-2 d-flex align-items-center">Upcoming Events</p>
      <%# Iterating through attending events %>
      <%= render '/shared/event_card_iteration' %>
      <div>
        <%= link_to "create event", new_event_path %>
        <%= link_to "see a user", user_path(@users.last) %>
        <%= link_to "my dashboard", dashboard_path(current_user) %>
      </div>
    </div>
  </div>
</div>
